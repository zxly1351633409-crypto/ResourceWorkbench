# 开发日志

## 2026-06-26

- 建立 `ResourceWorkbench` 项目目录。
- 拉取 QRoundedFrame 到 `external/QRoundedFrame`，作为后续 UI 壳参考。
- 检查本机环境：
  - Python 3.13 可用。
  - Git 已安装到系统目录。
  - 官方 7-Zip 下载源暂时连接失败。
  - 本机 `E:\Haozip\HaoZipC.exe` 可用，可作为临时压缩包后端。
- 完成第一版只读扫描原型：
  - 指定路径扫描。
  - 文件类型统计。
  - 按第一层资源文件夹生成临时卡片建议。
  - 标记混合资源和内部压缩包。
  - 输出 Markdown/JSON 报告。
- 增加两个双击脚本：
  - `检查工具.bat`
  - `启动-只读分析.bat`
- 修正报告文件名冲突问题，避免同一秒生成的报告互相覆盖。

下一步：

1. 用用户提供的小测试批次验证识别规则。
2. 根据真实误判修改分组和分类规则。
3. 增加“临时解压工作区”，仍然不移动/删除源资源。
4. 再考虑翻译、整理结构、移动、115 上传。

## 2026-06-26 第二轮

- 确认 7-Zip 已安装在 `C:\Program Files\7-Zip\7z.exe`。
- 用测试目录验证外层只有压缩包时，单纯看文件夹会过于保守。
- 增加“只读预览压缩包目录”能力：
  - 默认最多预览 12 个压缩包。
  - 每个压缩包最多读取 300 条目录项。
  - 分卷压缩包只预览 part1/001 入口。
  - 仍然不解压、不移动、不删除。
- 分类器现在会参考压缩包目录中的 `.blend`、`.fbx`、`.obj`、照片数量、内部二级压缩包等信息。
- 增加“可能需要拆分的子资源候选”提示，用于识别大合集类资源。
