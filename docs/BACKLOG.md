# 历史待办清单（归档）· 2026-06-29

> 归档提示（2026-07-11）：多数条目已经完成或被新的安全设计替代。当前任务与完成状态以 `HANDOFF_NEXT.md` 和 `RESOURCE_WORKBENCH_REQUIREMENTS.md` 为准，不要从本文件恢复已移除的旧 UI 或自动解压行为。

下次直接从这里挑。预估为粗略相对工作量；“可自验”= 我能在沙箱用编译+单测+CLI 验证；“需实跑”= 必须你在 Windows 上看效果。

## 已完成（参考）
扫描/拆卡 + 安全网防漏 · 预览选择 v2 · DeepSeek 结构化翻译 / 一键 / 批量 · 目标选择器 · 翻译后重命名(可撤销) · 测试移动(校验+可回滚) · 多选 + 多选模式 · 审阅队列面板 · 操作记录/撤销面板 · 统一显示格式(封面+工程) · 资源查重（CLI + GUI） · 空目录清理（CLI + GUI） · Logo · 重排防抖 · 移除115 UI · 待整理分析卡住修复。

## 高优先

1. **已完成：正式 Z 盘移动安全流程（#28）** — 2026-06-30。需你小批量实跑。
   - `mover` 已拆成预演 `plan_move` + 执行 `execute_move`，正式移动走 `execute_formal_move`；
   - GUI 设置里新增“开启正式 Z 盘移动”；正式移动来源默认允许所有已挂载盘符，也可关闭后手动填白名单；
   - 正式移动前会展示落点/文件数/容量，二次确认输入 `MOVE` 后才执行；
   - CLI `move --formal --all-source-roots --dry-run` 可用所有盘符来源只看落点；移动日志仍支持撤销。

2. **已完成一轮：拆分边界打磨（#27）** — 2026-06-30。后续仍需你的错分样本。
   - 修复“深层空目录吞掉外层资源”的包装层判断问题，MECHANICAL LEG 不再靠兜底恢复；
   - `possible_split_count` 不再把单个候选根误报为“可能拆分”；
   - 自动拆分仍要求至少 3 个有实际载荷的子资源，避免把单资源按格式目录拆碎。

3. **已完成第一版：QFluentWidgets / Fluent 皮肤** — 2026-06-30。需你实跑看效果。
   - 新增可选依赖 `PySide6-Fluent-Widgets`，未安装时自动使用内置 Fluent QSS；
   - 设置里可切换 Fluent 深色 / Fluent 浅色 / 经典浅色，并可改强调色；
   - 顶部、侧栏、卡片、对话框、菜单、滚动条都已走新皮肤。

## 中优先（多为 GUI 入口补全，CLI 已可用）

4. **已完成：查重 / 清理 的 GUI 入口** — 2026-06-30。
   - 工具栏已加查重、空目录清理按钮；查重只列结果不删除；清理先预览、二次确认后只删除真正空目录。

5. **已完成：批量“整理为封面+工程”** — 2026-06-30。
   - 多选模式新增“整理选中”，统一执行 `formatter.apply_cover_project`，带确认、进度与成功/失败汇总。

6. **已完成：资源卡片备注 / 手动标签 + 按标签筛选** — 2026-06-30。
   - 卡片右键新增“备注 / 标签”；数据写入本地 `card_metadata.sqlite`；
   - 搜索框支持普通关键词匹配备注/标签，也支持 `#标签` 只按人工标签筛选。

7. **已完成：启动概览页 / 看板** — 2026-06-30。
   - 顶部新增概览按钮；
   - 展示当前卡片数、待确认、已移动、容量、类型/置信度/标签分布、拆分/兜底状态、移动模式和最新报告。

## 低优先 / 待条件

8. **115 上传（#29）** — 待你给开放平台 AppID/AppSecret。
   - 补 `uploader_115` 的 OAuth/token + `_upload_file`（建目录→直传凭证→sha1 秒传/分片）+ 限速/分批/退避防风控；恢复 UI 入口。

9. **已完成：移动后空目录清理联动** — 2026-06-30。
   - 设置里可开启；移动成功后只沿原来源父级链路清理真正空的目录，不扫整盘。

10. **Windows 端到端验收（#24）** — 你启动逐项确认；统计兜底卡占比反哺 #27。

## 约定（不要打破）
- 不自动解压；删除仅限“空目录”；
- 翻译/分类/整理均为建议或可撤销；正式 Z 盘移动已实现但默认关闭，来源可设为所有盘符或手动白名单，必须预演 + 二次确认 `MOVE`；删除运输包、115 真实上传仍未开放。
- 改 src 大文件后必跑 `python -m compileall src` + `python -m unittest discover -s tests`；GUI 改动用 PySide6 桩导入自检，真效果需 Windows 实跑。
- 复盘问题优先读 `reports/*.json`（用户真实运行结果），别只靠本地副本猜。
